import "./home.css"
// import 

const Home = () => {
    return (
        <>
            {/* <button onClick={toggleTheme}> */}
                {/* Toggle to {mode === 'light' ? 'Dark' : 'Light'} Mode */}
            {/* </button> */}
            <div>
                <h1>Welcome to My Themed Website!</h1>
            </div>
        </>
    )
};

export default Home;